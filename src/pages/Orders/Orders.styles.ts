import {styled} from "@mui/material";
import {PageContainer} from "components/PageContainer";

export const OrdersPage = styled(PageContainer)(() => ({}))

export const PageHeaderContainer = styled('div')(() => ({
    display: 'flex',
    padding: '24px 0 16px',
    justifyContent: 'space-between'
}))
